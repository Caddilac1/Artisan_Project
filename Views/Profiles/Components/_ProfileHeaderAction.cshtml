@model ArtisanMarketplace.Models.AppUser

 <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <!-- Page Header -->
        <div class="mb-8">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between">
                <div>
                    <h1 class="text-fluid-3xl font-semibold text-text-primary">Profile Settings</h1>
                    <p class="mt-2 text-text-secondary">Manage your public profile and account preferences.</p>
                </div>
                <div class="mt-4 sm:mt-0 flex space-x-3">
                    <button class="inline-flex items-center px-4 py-2 border border-secondary-300 text-text-secondary font-medium rounded-lg hover:bg-secondary-100 transition-colors btn-scale">
                        Cancel
                    </button>
                    <button class="inline-flex items-center px-6 py-2 bg-primary text-white font-medium rounded-lg hover:bg-primary-700 transition-colors btn-scale fab">
                        Save Changes
                    </button>
                </div>
            </div>
        </div>
